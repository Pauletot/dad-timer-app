// db.js
// This is where the IndexedDB logic will live: opening the database,
// defining the "patterns" and "steps" stores, and CRUD functions.
// We'll fill this in during the database step — left empty on purpose for now.

export {};
